export class Search {
    id: string;
    startDate: string;
    endDate: string;
  
    constructor(
      id: string,
      startDate: string,
      endDate: string
    ) {
        this.id = id,
        this.startDate = startDate,
        this.endDate = endDate
  
    }
  }
  